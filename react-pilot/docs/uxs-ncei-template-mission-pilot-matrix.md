# NCEI UxS template ↔ mission wizard ↔ XML import matrix

**Canonical:** the **mission profile wizard** in `react-pilot` and its `**pilotState`** JSON are the reference for field names, validation, XML preview, and browser import.

This document maps the [NCEI Uncrewed Systems (UxS) Metadata Templates](https://www.ncei.noaa.gov/products/uncrewed-system-metadata-templates) (overview PDF + ISO 19115-2 GMI XML) to those `**pilotState`** paths, and records what `**importPilotPartialStateFromXml`** (`src/lib/xmlPilotImport.js`) reads back.

**Sources**

- **Validation rules (generated table):** [generated/mission-validation-rules.generated.md](./generated/mission-validation-rules.generated.md) — every `missionValidationRules.js` rule with modes, `pilotState` field, severity, message, XPath. Regenerate: `npm run docs:mission-validation-rules` from `react-pilot/`.
- **Validation by area (human table):** [mission-validation-by-area.md](./mission-validation-by-area.md) — GCMD facets → required/UUID rules; sensors ↔ keywords; mission/platform/spatial/distribution summaries; lenient/strict/catalog extras.
- **Validation by field (generated matrix):** [generated/mission-validation-by-field.generated.md](./generated/mission-validation-by-field.generated.md) — each `pilotState` field with all rules that target it. Regenerate: `npm run docs:mission-validation-rules`.
- Runtime / hosting: [DEPLOYMENT.md](./DEPLOYMENT.md) (HTTP `/api/db`).
- Human-readable walkthrough of the same GMI XML sections (root → acquisition): [uxs-ncei-gmi-xml-narrative-overview.md](./uxs-ncei-gmi-xml-narrative-overview.md).
- USX V4 Excel / classic wizard field names vs canonical `pilotState` + gaps: [uxs-v4-excel-pilot-field-map.md](./uxs-v4-excel-pilot-field-map.md).
- Row-level placeholder text from the NCEI workbook is extracted to [uxs-ncei-placeholder-xlsx-crosswalk.md](./uxs-ncei-placeholder-xlsx-crosswalk.md) (regenerate with `npm run docs:placeholders` from `react-pilot/` when the `.xlsx` changes).
- NCEI product page (beta templates, contacts, legislation).
- [UxS Metadata Template Overview (PDF)](https://www.ncei.noaa.gov/sites/default/files/2026-03/UxS%20Metatdata%20Template%20Overview.pdf) — section headings used in the first column below.
- Local fixture: `NOAANavyUxSAcquisition_MetadataTempate_19115-2-GMI_2026-2 (1).xml` (same lineage as the published `…2026-2.xml`).
- **Exemplar (same matrix, different filled record):** *UxS METADATA TEMPLATE, 2025 BETA* — e.g. **Saildrone PMEL TPOS 2021** (`gmi:MI_Metadata` with Saildrone/PMEL narrative, TPOS aggregations, GCMD facets, acquisition `MI_Platform` / `MI_Instrument`). It is **not** a second matrix: each XML block maps to **one row** in the [section matrix](#section-matrix) (root & header, grid, citation, description, keywords, constraints, aggregations, extent, POC, contentInfo, distribution, data quality, maintenance, topic category, graphic overview, acquisition). After import, validation is exactly the rules in [generated/mission-validation-rules.generated.md](./generated/mission-validation-rules.generated.md). Typical beta issues: unreplaced `{{…}}` tokens, invalid calendar dates, and `gmd:URL` values that are not yet http(s) — those import as literals and fail the corresponding checks.

**Wizard steps** (mission profile): `1. Mission` → `2. Platform` → `3. Sensors` → `4. Spatial` → `5. Keywords` → `6. Distribution`.

**Explicit scope:** **Aggregations** in the wizard are flat fields → simplified XML blocks (not every nested citation shape in the full NCEI XML template). **Validation:** React `validatePilotState` / profile rules and GAS `SchemaValidator.gs` are updated together by convention, not by automatic sync — see [uxs-v4-excel-pilot-field-map.md](./uxs-v4-excel-pilot-field-map.md) (aggregation + strict rows).

---

## Legend


| Import      | Meaning                                                                                                 |
| ----------- | ------------------------------------------------------------------------------------------------------- |
| **Full**    | Populated from typical UxS XML when the element is present and matches the shapes the importer expects. |
| **Partial** | Some subfields map; others are dropped, merged into one field, or depend on element order / encoding.   |
| **None**    | Not read by `importPilotPartialStateFromXml` today.                                                     |


---

## Exemplar vs matrix (2025 beta Saildrone / PMEL TPOS)

Stakeholders sometimes ship a **fully filled** 2025 beta template (Saildrone, PMEL TPOS, etc.) as the “gold” example. That file uses the **same ISO element groups** as the table below; the matrix describes **mapping + import depth**, not a separate checklist per vendor.

| Your XML block (exemplar) | Section matrix row(s) | After import, validate with |
| ------------------------- | ------------------------ | ---------------------------- |
| `fileIdentifier`, `dateStamp`, root `contact`, `language`, `hierarchyLevel`, `spatialRepresentationInfo` (grid) | **1. Root & header**, **2. Spatial representation (grid)** | `mission.*`, `spatial.*`, `distribution.*` rules in [generated/mission-validation-rules.generated.md](./generated/mission-validation-rules.generated.md) |
| `identificationInfo` / citation, abstract, purpose, status, POC, maintenance, graphicOverview, keywords, constraints, aggregations, extent, topicCategory, supplemental | **3.A–3.F**, **3 (POC)**, **Topic category**, **Graphic overview** | Same generated table (`mission.*`, `keywords.*`, etc.) |
| `contentInfo` / `MI_CoverageDescription` | **4. Content information** | Sensor-related rules if content fills `sensors[]`; acquisition may supersede |
| `distributionInfo` | **5. Distribution** | `distribution.*` rules (URLs, format, license, fees text, …) |
| `dataQualityInfo`, `metadataMaintenance` | **6. Data quality**, **6. Maintenance** | `spatial.*` partial lineage rules; `distribution.metadataMaintenanceFrequency` |
| `acquisitionInformation` / platform & instruments | **7. Acquisition (UxS)** | `platform.*`, `sensors[]` rules |

---

## Section matrix


| NCEI overview / template area        | Representative ISO paths                                                                                                                                                  | `pilotState` (primary paths)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Wizard | Import      | Notes                                                                                                                                                                                                                                                                                                                |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1. Root & header**                 | `gmi:MI_Metadata` / `gmd:fileIdentifier`, `gmd:dateStamp`, `gmd:hierarchyLevel`, `gmd:metadataStandardName` / `Version`, root `gmd:contact`                               | `mission.fileId`, `mission.metadataRecordDate`, `mission.scopeCode` (from root `hierarchyLevel` code); `distribution.metadataStandard`, `distribution.metadataVersion`; root `gmd:contact` xlink → `distribution.nceiMetadataContactHref` / `nceiMetadataContactTitle`, `useNceiMetadataContactXlink`                                                                                                                                                                                                           | 1 / 6  | **Full**    | Standard name/version stay on `distribution`. DocuComp-only root contact sets the same fields the Distribution step uses for xlink export.                                                                                                                                                                           |
| **2. Spatial representation (grid)** | `gmd:spatialRepresentationInfo` / `MD_GridSpatialRepresentation`, `axisDimensionProperties`, `cellGeometry`; `MD_Georectified`                                            | `spatial.useGridRepresentation`, `spatial.gridCellGeometry`, column/row/vertical size & resolution, `spatial.dimensions` (georectified path)                                                                                                                                                                                                                                                                                                                                                                    | 4      | **Full**    | Matches template grid block.                                                                                                                                                                                                                                                                                         |
| **3.A Citation**                     | `…/MD_DataIdentification/gmd:citation/CI_Citation` — `title`, `alternateTitle`, `date` (creation/publication), `identifier`, `citedResponsibleParty`                      | `mission.title`, … `mission.accession`; `mission.citationAuthorIndividualName`, `citationAuthorOrganisationName`, `citationPublisherOrganisationName`, `citationOriginatorIndividualName`, `citationOriginatorOrganisationName`                                                                                                                                                                                                                                                                                 | 1      | **Full**    | **DOI** / **Accession** rules unchanged. **Citation parties**: first `author` / `publisher` / `originator` by `CI_RoleCode`; preview re-emits the same roles.                                                                                                                                                        |
| **3.B Description**                  | `abstract`, `purpose`, `status`, `supplementalInformation`                                                                                                                | `mission.abstract`, `mission.purpose`, `mission.status`, `mission.supplementalInformation`                                                                                                                                                                                                                                                                                                                                                                                                                      | 1      | **Full**    |                                                                                                                                                                                                                                                                                                                      |
| **3.B Language / charset**           | `MD_DataIdentification/gmd:language`, `gmd:characterSet`                                                                                                                  | `mission.language`, `mission.characterSet`                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 1      | **Partial** | `language`: reads `gmd:LanguageCode` **or** falls back to `gco:CharacterString` (UxS `eng; USA` pattern). `characterSet`: only when `MD_CharacterSetCode` present under identification.                                                                                                                              |
| **3.C Keywords (GCMD)**              | `descriptiveKeywords` / `MD_Keywords` + `thesaurusName` title                                                                                                             | `keywords.*` (`label` + `uuid`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 5      | **Full**    | Facet by thesaurus title substring (skips **platform instance**). `**gmx:Anchor`**: label + `uuid` from `/concept/{uuid}`, `?uuid=`, or full URL; preview emits `**gmx:Anchor`** when `uuid` is set.                                                                                                                 |
| **3.D Constraints**                  | `resourceConstraints` / `MD_LegalConstraints` — `useLimitation`, `accessConstraints`, `otherConstraints`; DocuComp `xlink` on `MD_Constraints`                            | `mission.citeAs`, `mission.accessConstraints`, `mission.distributionLiability` (first prose `otherConstraints`), `mission.otherCiteAs` (second prose), `mission.licenseUrl` + preset when line starts `Data license:` ; `mission.dataLicensePreset` from DocuComp href                                                                                                                                                                                                                                          | 1      | **Partial** | `otherConstraints` nodes containing `**gmx:Anchor`** are skipped (license anchors).                                                                                                                                                                                                                                  |
| **3.E Aggregation**                  | `aggregationInfo` / `MD_AggregateInformation` + association/initiative codes                                                                                              | `mission.parentProject`*, `mission.relatedDataset*`, `mission.relatedDataUrl*`, `mission.associatedPublication*`                                                                                                                                                                                                                                                                                                                                                                                                | 1      | **Full**    | `parseAggregations` + preview cover the common UxS branches; citation / party richness stays **simpler** than an arbitrary-depth workbook template.                                                                                                                                                                  |
| **3.F Extent**                       | `extent` / `EX_Extent` — descriptions, bbox, temporal (`TimePeriod`), vertical                                                                                            | `mission.west/east/south/north`, `mission.vmin/vmax`, `mission.startDate/endDate`, `temporalExtentInterval`*, `spatial.geographicDescription`, `spatial.verticalCrsUrl`, `spatial.hasTrajectory`, `spatial.trajectorySampling`                                                                                                                                                                                                                                                                                  | 1 / 4  | **Full**    | Geographic free text (non-vertical, non-trajectory lines) → `spatial.geographicDescription`. Vertical CRS from description line prefix `Vertical CRS:` . Trajectory from `Trajectory sampling:` .                                                                                                                    |
| **3 (POC)**                          | `MD_DataIdentification/gmd:pointOfContact`                                                                                                                                | `mission.individualName`, `mission.org`, `mission.email`, `mission.contactPhone`, `mission.contactUrl`, `mission.contactAddress`                                                                                                                                                                                                                                                                                                                                                                                | 1      | **Full**    | Importer walks **all** `pointOfContact` elements and uses the first that contains `CI_ResponsibleParty`, skipping preceding DocuComp-only stubs.                                                                                                                                                                     |
| **4. Content information**           | `gmd:contentInfo` / `gmi:MI_CoverageDescription`, `MD_Band`                                                                                                               | `sensors[]` (when **no** acquisition instruments win)                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 3      | **Partial** | `parseSensors` reads `MI_CoverageDescription`: identifier code, `attributeDescription`, `name`, `description` (parsed via `parseInstrumentDescriptionBlock`). Used only if acquisition path yields no sensors.                                                                                                       |
| **5. Distribution**                  | `MD_Distribution` / `MD_Distributor` — `distributorFormat`, `distributorTransferOptions`, `distributionOrderProcess`, `distributorContact` xlink or `CI_ResponsibleParty` | Same as before plus `distribution.distributionFeesText`, `distributionOrderingInstructions`, `nceiDistributorContactHref` / `nceiDistributorContactTitle`; **inline** distributor party → `distributorIndividualName`, `distributorOrganisationName`, `distributorEmail`, `distributorContactUrl` when no xlink; `onLine` → `metadataLandingUrl` / `landingUrl` / `downloadUrl` plus `metadataLandingLinkName`, `metadataLandingDescription`, `downloadLinkName`, `downloadLinkDescription`, `downloadProtocol` | 6      | **Partial** | **http(s)** URLs only, no `{{…}}`. **Slot order**: `CI_OnlineResource` rows are sorted by name/description/protocol/URL keywords (metadata vs download) before the 1 / 2 / ≥3 slotting so mis-ordered template links still map sensibly. **Fees** / **ordering** from `MD_StandardOrderProcess` under distributor.   |
| **6. Data quality**                  | `dataQualityInfo` / `DQ_DataQuality` / quantitative reports + `lineage` / `LI_Lineage`                                                                                    | `spatial.accuracyStandard`, `spatial.accuracyValue`, `spatial.errorLevel`, `spatial.errorValue`; `**spatial.lineageStatement`**, `**spatial.lineageProcessSteps`** (process `description` elements, blank-line separated round-trip)                                                                                                                                                                                                                                                                            | 4      | **Partial** | Quantitative reports unchanged; **lineage** statement + process-step descriptions imported; richer `LI_ProcessStep` (sources, processors, dates) not modeled in `pilotState` yet.                                                                                                                                    |
| **6. Maintenance**                   | `resourceMaintenance` / `MD_MaintenanceInformation`                                                                                                                       | `distribution.metadataMaintenanceFrequency`                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 6      | **Full**    | Preview always emits `resourceMaintenance` (default `asNeeded`); import reads `MD_MaintenanceFrequencyCode`.                                                                                                                                                                                                         |
| **Topic category**                   | `gmd:topicCategory` / `MD_TopicCategoryCode`                                                                                                                              | `mission.topicCategories[]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 1      | **Full**    | Code strings such as `oceans`; one XML element per array entry in preview.                                                                                                                                                                                                                                           |
| **Graphic overview**                 | `gmd:graphicOverview` xlink                                                                                                                                               | `mission.graphicOverviewHref`, `mission.graphicOverviewTitle`                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 1      | **Full**    | First xlink-only overview; preview emits self-closing `graphicOverview` when href set.                                                                                                                                                                                                                               |
| **7. Acquisition (UxS)**             | `gmi:acquisitionInformation` / `MI_AcquisitionInformation` — `MI_Platform`, `MI_Instrument`                                                                               | `platform.`*, `sensors[]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2 / 3  | **Full**    | Platform: id, type, description (incl. `Model:` lines → `platform.model`), manufacturer from platform `pointOfContact` org, `otherProperty` characteristic list (weight, dimensions, material, speed, operational area). Instruments: top-level and nested under platform; deduped with `sensorInstrumentDedupeKey`. |


---

## `pilotState` areas not driven by this template import

These mission wizard fields are **not** filled from `importPilotPartialStateFromXml` (export may still emit them from the preview generator):

- `mission.ror`, `mission.metadataUuid`, hierarchy-level **name** (template uses root scope code only).
- `platform.platformName`, serial number, deployment date, navigation, sensor mounts, many extended platform text fields — unless present in acquisition `description` / `otherProperty` paths above.
- Full `**sensors[]`** optional XML fields: only those understood by `parseInstrumentDescriptionBlock` + acquisition columns.
- **Distributor party** when encoded as DocuComp **xlink** only (no embedded `CI_ResponsibleParty`) still maps to href/title only, not a full party record.

---

## Suggested next engineering steps

1. **Cross-check xlsx:** NCEI’s [Metadata Placeholders and Script Generation.xlsx](https://www.ncei.noaa.gov/sites/default/files/2026-03/Metadata%20Placeholders%20and%20Script%20Generation.xlsx) row list against this table when you need row-level parity.
2. **Citation depth:** Optional support for multiple authors or full `CI_Contact` on citation parties (today: first author / publisher / originator names only).

---

## Code anchor

Single entry point for the import column: `importPilotPartialStateFromXml` in `[src/lib/xmlPilotImport.js](../src/lib/xmlPilotImport.js)`.