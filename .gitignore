# Local Netlify
.netlify/

# Dependencies
node_modules/

# Build / publish outputs
dist/
dist-ssr/
pilot-share/

# Environment and secrets (tracked examples: **/.env.example)
.env
.env.local
.env.*.local
mcp/noaa-metadata/config/oauth-client.json
mcp/noaa-metadata/config/allowlist.json

# Archives (local exports / backups — do not publish)
*.zip
cruisepack_extracted/

# Logs and OS noise
*.log
.DS_Store
Thumbs.db

# Editor (optional — uncomment if you never commit IDE settings)
# .vscode/
# .idea/
